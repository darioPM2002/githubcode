import 'package:flutter/material.dart';

class LoayaltyScreen extends StatelessWidget {
  const LoayaltyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Loyalty Screen"),),
    );
  }
}